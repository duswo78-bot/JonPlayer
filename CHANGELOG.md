# JonPlayer Development History (CHANGELOG)

## [2026-06-05]

### Added
- **네트워크 스트리밍 및 YouTube 재생 지원**: `Ctrl + U` 단축키를 통해 유튜브 링크나 네트워크 스트림(RTSP, HTTP 등) URL을 직접 입력해 즉시 재생할 수 있는 기능을 추가했습니다.
- **PIP (Picture-in-Picture) 모드**: 영상을 보면서 다른 작업을 할 수 있도록 최상단 미니 윈도우로 분리되는 PIP 모드를 추가했습니다. (`P` 키로 전환, 50%/100%/200% 창 크기 조절 기능 포함)
- **인스톨러(설치 프로그램) 배포 지원**: Inno Setup을 기반으로 `JonPlayer.iss` 스크립트를 작성하여, 사용자 동의(EULA) 및 인증(Registration) 화면을 거쳐 윈도우에 정식 설치될 수 있는 패키징 시스템을 구축했습니다.
- **HLSL 셰이더 기반 비디오 필터**: 기존의 무거운 CPU 기반 소프트웨어 필터를 제거하고, 하드웨어 가속(GPU)을 사용하는 고성능 HLSL 셰이더 필터(`H` 키)를 도입했습니다.

### Changed
- **다크/라이트 테마 전역 최적화**: 플레이어 메인 화면뿐만 아니라 URL 입력창(`InputWindow`) 등 모든 팝업 대화상자에도 라이트/다크 테마가 동적으로 완벽히 상속되도록 리소스 바인딩 구조를 개선했습니다.
- **자막 추출 로직 개선**: 스트리밍 영상의 경우 AI 자막(Whisper) 추출이 원천적으로 불가능하므로, 오작동을 방지하기 위해 스트리밍 URL 재생 시에는 CC+ 버튼이 자동으로 비활성화되도록 수정했습니다.

### Fixed
- **단축키 충돌 및 잔여 오류 해결**: 작동하지 않거나 중복 할당되었던 미디어 필터용 단축키(`W, S, E, D, R, F`)의 내부 찌꺼기 로직을 완전히 제거하여 사용자 혼선을 방지했습니다.
- **PIP 창 크기 조절 버그 픽스**: PIP 모드 진입 시 메인 창의 최소 크기 제약(MinWidth=600)을 동적으로 해제하여, 미니 화면 사이즈 축소가 정상적으로 작동하도록 수정했습니다.

## [2026-05-31]

### Added
- **스플래시 스크린 (배경 로고)**: 영상이 재생 중이지 않거나 정지(Stop) 상태일 때 `BG logo.png`가 화면 중앙에 적절한 비율로 꽉 차게 나타나도록 구현했습니다.
- **개발 이력 관리**: 일일 단위의 개발 내역을 기록하는 `CHANGELOG.md`를 도입하여 향후 업데이트 내역을 한눈에 파악할 수 있도록 했습니다.

### Changed
- **상단 타이틀 바 디자인 개선**: 'Jon'과 'Player' 텍스트 사이의 띄어쓰기를 없애고, 'Player' 부분에 시안색에서 파란색으로 이어지는 오리지널 그라데이션 컬러를 완벽히 구현했습니다.
- **슬라이더 탐색 로직 전면 개편**: WPF `Slider`의 자체 이벤트를 활용해 썸(Thumb) 드래그와 트랙(Track) 클릭 시의 처리를 완벽하게 분리했습니다. (드래그 중인 경우에는 탐색을 지연시켰다가 손을 뗄 때 반영하여 UI가 덜덜거리는 현상을 제거했습니다)
- **Dispose 체인 (메모리 해제) 안정성 대폭 강화**:
  - `Decoder`, `Renderer`, `Texture`, `FrameBuffer`, `Thread`의 모든 자원 해제 라이프사이클을 추적 검증했습니다.
  - `Dispose`를 두 번 호출해도 죽지 않도록 `_isDisposed` 플래그 및 방어 코드를 추가했습니다.
  - 별도로 `Stop()`을 부르지 않아도 `Dispose()` 시 알아서 스레드와 메모리를 강제 회수하도록 구현했습니다.

### Fixed
- **짧은 영상 (키프레임 부족 영상) 점프 불가 문제**: 8초짜리 같은 짧은 영상에서 원하는 곳을 찍으면 0초로 돌아가는 문제를 수정했습니다. FFmpeg가 과거의 키프레임(0초)으로 점프하더라도 목표 시간에 도달할 때까지 불필요한 프레임들을 렌더링 없이 초고속으로 버리도록(Discard) 디코딩 루프를 수정하여 즉각적인 점프가 가능해졌습니다.
- **정지 (Stop) 시 잔상 문제 및 프로그레스 바 꼬임 해결**:
  - `Stop` 버튼을 누르면 마지막으로 출력된 비디오 프레임 레이어가 즉시 사라져 잔상이 남지 않게 수정했습니다.
  - 정지 후 내부 스레드의 잔여 이벤트가 `0`으로 초기화된 프로그레스 바를 덮어씌워 꼬이던 버그를 고쳤습니다.
- **자동 멈춤 (Auto-Stop) 누락 수정**: 파일의 끝(EOF)에 도달하면 `PlaybackFinished` 이벤트를 정상적으로 발동시켜 플레이어가 처음 상태로 깔끔하게 멈추도록 수정했습니다.
- **예외 발생 시 메모리 누수 방지**: `Open` 중에 호환되지 않는 파일 등으로 인해 에러가 발생하면, 불완전하게 초기화된 C++ 포인터 메모리들을 확실하게 찌꺼기 없이 `Cleanup()` 하도록 에러 핸들링을 추가했습니다.
