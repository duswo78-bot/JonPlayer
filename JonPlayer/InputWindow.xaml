<Window x:Class="JonPlayer.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Open Network URL" Height="150" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextBrush}"
        Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Enter Network Stream URL (e.g. http://... or rtsp://...):" Grid.Row="0" Margin="0,0,0,10"/>
        <TextBox x:Name="TxtUrl" Grid.Row="1" Height="25" Background="{DynamicResource PanelBrush}" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource BorderBrush}" VerticalContentAlignment="Center" Padding="5,0"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancel" Width="70" Height="25" Margin="0,0,10,0" Click="BtnCancel_Click" Background="{DynamicResource PanelBrush}" Foreground="{DynamicResource TextBrush}" BorderThickness="0"/>
            <Button Content="Open" Width="70" Height="25" Click="BtnOpen_Click" Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
